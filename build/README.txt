Drop a macOS `icon.icns`, Windows `icon.ico`, and Linux `icon.png` here
before running `npm run package`. electron-builder falls back to defaults
if any are missing, so the build still succeeds without them.
